{% extends 'base.html.twig' %}

{% block title %}SecurityIncident{% endblock %}

{% block body %}
    <h1>SecurityIncident</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ security_incident.id }}</td>
            </tr>
            <tr>
                <th>IncidentDate</th>
                <td>{{ security_incident.incidentDate ? security_incident.incidentDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ security_incident.description }}</td>
            </tr>
            <tr>
                <th>Severity</th>
                <td>{{ security_incident.severity }}</td>
            </tr>
            <tr>
                <th>Resolved</th>
                <td>{{ security_incident.resolved ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_security_incident_index') }}">back to list</a>

    <a href="{{ path('app_security_incident_edit', {'id': security_incident.id}) }}">edit</a>

    {{ include('security_incident/_delete_form.html.twig') }}
{% endblock %}
