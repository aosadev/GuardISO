<form method="post" action="{{ path('app_security_incident_delete', {'id': security_incident.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ security_incident.id) }}">
    <button class="btn">Delete</button>
</form>
