{% extends 'base.html.twig' %}

{% block title %}Risk{% endblock %}

{% block body %}
    <h1>Risk</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ risk.id }}</td>
            </tr>
            <tr>
                <th>RiskDescription</th>
                <td>{{ risk.riskDescription }}</td>
            </tr>
            <tr>
                <th>Likelihood</th>
                <td>{{ risk.likelihood }}</td>
            </tr>
            <tr>
                <th>Impact</th>
                <td>{{ risk.impact }}</td>
            </tr>
            <tr>
                <th>MitigationMeasures</th>
                <td>{{ risk.mitigationMeasures }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_risk_index') }}">back to list</a>

    <a href="{{ path('app_risk_edit', {'id': risk.id}) }}">edit</a>

    {{ include('risk/_delete_form.html.twig') }}
{% endblock %}
